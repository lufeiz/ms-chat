---
"@ms-chat/react": patch
"@ms-chat/vue": patch
---

无障碍（a11y）：

- Sender 的发送/停止/上传由裸 `<div onClick>` 改为 `role="button"` + `tabindex` + `aria-label` + Enter/Space 键盘激活，键盘与读屏用户现可操作；输入框补 `aria-label`。
- 消息列表容器加 `role="log"` + `aria-live="polite"`，流式助手回复会被读屏增量播报。
