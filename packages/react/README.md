# @ms-chat/react

MS-Chat 的 React 组件包：一个 `<MSChat>` 容器即可接入会话列表、流式对话、文件上传与多端模式（PC / H5 / 气泡）。

```bash
npm i @ms-chat/react
```

```tsx
import { MSChat } from '@ms-chat/react';
```

快速开始、props 配置、架构图与流式接入示例见仓库根 [README](../../README.md)。

MIT License.
